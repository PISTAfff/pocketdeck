# Hoist Next.js eslint plugin deps so the flat config can resolve them via FlatCompat
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*types*
public-hoist-pattern[]=*next*
# Avoid pnpm strict peer resolution failures during scaffold
strict-peer-dependencies=false
auto-install-peers=true
