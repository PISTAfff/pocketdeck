'use client';

/**
 * ChromeRoot — PHASE 1 PLACEHOLDER.
 *
 * Phase 2C replaces this file with the persistent chrome:
 *   - <CustomCursor />, <MagneticButton /> wiring
 *   - Lenis smooth-scroll provider
 *   - Page transition wrapper (Framer Motion overlay over a persistent canvas)
 *   - <Nav /> at the top, <Footer /> at the bottom of children
 *
 * Do not edit this placeholder from Phase 2B — only Phase 2C owns this file.
 */
export function ChromeRoot({ children }: { children: React.ReactNode }) {
  return <div className="page-root">{children}</div>;
}
